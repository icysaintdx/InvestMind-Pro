# 📱 AlphaCouncil 前端集成指南

## 需要创建的Vue组件

### 1. 新闻分析组件
路径: `alpha-council-vue/src/components/NewsAnalysis.vue`

```vue
<template>
  <div class="news-analysis">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>📰 新闻舆情分析</span>
          <el-button type="primary" @click="fetchNews">刷新</el-button>
        </div>
      </template>
      
      <el-form inline>
        <el-form-item label="股票代码">
          <el-input v-model="stockCode" placeholder="如: 600519"></el-input>
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="analyzeNews">分析情绪</el-button>
        </el-form-item>
      </el-form>
      
      <div v-if="sentiment" class="sentiment-result">
        <el-progress
          :percentage="sentiment.score * 100"
          :color="sentimentColor"
          :format="formatSentiment"
        />
      </div>
      
      <el-table :data="newsList" style="margin-top: 20px">
        <el-table-column prop="title" label="标题" />
        <el-table-column prop="source" label="来源" width="120" />
        <el-table-column prop="publish_time" label="时间" width="180" />
        <el-table-column label="情绪" width="100">
          <template #default="{ row }">
            <el-tag :type="getSentimentType(row.sentiment)">
              {{ row.sentiment }}
            </el-tag>
          </template>
        </el-table-column>
      </el-table>
    </el-card>
  </div>
</template>

<script>
import axios from 'axios'

export default {
  name: 'NewsAnalysis',
  data() {
    return {
      stockCode: '600519',
      newsList: [],
      sentiment: null
    }
  },
  computed: {
    sentimentColor() {
      if (!this.sentiment) return '#909399'
      const score = this.sentiment.score
      if (score > 0.6) return '#67C23A'
      if (score > 0.4) return '#E6A23C'
      return '#F56C6C'
    }
  },
  methods: {
    async fetchNews() {
      try {
        const response = await axios.get(`/api/news/list`, {
          params: { stock_code: this.stockCode, limit: 20 }
        })
        this.newsList = response.data.news
      } catch (error) {
        this.$message.error('获取新闻失败')
      }
    },
    async analyzeNews() {
      try {
        const response = await axios.post(`/api/news/analyze`, {
          stock_code: this.stockCode,
          days: 7
        })
        this.sentiment = response.data.sentiment
        this.$message.success('情绪分析完成')
      } catch (error) {
        this.$message.error('分析失败')
      }
    },
    formatSentiment(percentage) {
      return `${percentage.toFixed(0)}% ${percentage > 60 ? '😊' : percentage > 40 ? '😐' : '😰'}`
    },
    getSentimentType(sentiment) {
      if (sentiment === 'positive') return 'success'
      if (sentiment === 'negative') return 'danger'
      return 'info'
    }
  },
  mounted() {
    this.fetchNews()
  }
}
</script>
```

### 2. 多智能体辩论组件
路径: `alpha-council-vue/src/components/AgentDebate.vue`

```vue
<template>
  <div class="agent-debate">
    <el-row :gutter="20">
      <el-col :span="12">
        <el-card>
          <template #header>
            <span>🐂 看涨方观点</span>
          </template>
          <div class="debate-content">
            <h4>信心度: {{ bullConfidence }}%</h4>
            <ul>
              <li v-for="point in bullPoints" :key="point">{{ point }}</li>
            </ul>
          </div>
        </el-card>
      </el-col>
      
      <el-col :span="12">
        <el-card>
          <template #header>
            <span>🐻 看跌方观点</span>
          </template>
          <div class="debate-content">
            <h4>信心度: {{ bearConfidence }}%</h4>
            <ul>
              <li v-for="point in bearPoints" :key="point">{{ point }}</li>
            </ul>
          </div>
        </el-card>
      </el-col>
    </el-row>
    
    <el-card style="margin-top: 20px">
      <template #header>
        <span>🎯 最终决策</span>
      </template>
      <el-result
        :icon="decisionIcon"
        :title="decisionTitle"
        :sub-title="decisionSubtitle"
      >
        <template #extra>
          <el-button type="primary" @click="startDebate">开始新辩论</el-button>
        </template>
      </el-result>
    </el-card>
  </div>
</template>
```

### 3. 模拟交易组件
路径: `alpha-council-vue/src/components/TradingSimulator.vue`

```vue
<template>
  <div class="trading-simulator">
    <el-row :gutter="20">
      <el-col :span="8">
        <el-statistic title="总资产" :value="portfolio.total_value" prefix="¥" />
      </el-col>
      <el-col :span="8">
        <el-statistic 
          title="总收益率" 
          :value="portfolio.total_profit_loss_rate" 
          suffix="%"
          :value-style="profitStyle"
        />
      </el-col>
      <el-col :span="8">
        <el-statistic title="持仓数量" :value="portfolio.positions_count" />
      </el-col>
    </el-row>
    
    <el-table :data="positions" style="margin-top: 20px">
      <el-table-column prop="stock_name" label="股票" />
      <el-table-column prop="quantity" label="数量" />
      <el-table-column prop="avg_cost" label="成本" />
      <el-table-column prop="current_price" label="现价" />
      <el-table-column label="盈亏">
        <template #default="{ row }">
          <span :style="{ color: row.profit_loss > 0 ? '#67C23A' : '#F56C6C' }">
            {{ row.profit_loss > 0 ? '+' : '' }}{{ row.profit_loss.toFixed(2) }}
            ({{ row.profit_loss_rate.toFixed(2) }}%)
          </span>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>
```

### 4. 闭环验证组件
路径: `alpha-council-vue/src/components/VerificationSystem.vue`

```vue
<template>
  <div class="verification-system">
    <el-tabs v-model="activeTab">
      <el-tab-pane label="决策记录" name="decisions">
        <el-timeline>
          <el-timeline-item
            v-for="decision in decisions"
            :key="decision.decision_id"
            :timestamp="decision.timestamp"
            placement="top"
          >
            <el-card>
              <h4>{{ decision.stock_name }} - {{ decision.recommendation }}</h4>
              <p>信心度: {{ (decision.confidence * 100).toFixed(0) }}%</p>
              <p>目标价: ¥{{ decision.target_price }}</p>
            </el-card>
          </el-timeline-item>
        </el-timeline>
      </el-tab-pane>
      
      <el-tab-pane label="验证结果" name="verifications">
        <el-table :data="verifications">
          <el-table-column prop="decision_id" label="决策ID" />
          <el-table-column prop="accuracy_rate" label="准确率">
            <template #default="{ row }">
              {{ (row.accuracy_rate * 100).toFixed(1) }}%
            </template>
          </el-table-column>
          <el-table-column prop="profit_loss" label="盈亏">
            <template #default="{ row }">
              <el-tag :type="row.profit_loss > 0 ? 'success' : 'danger'">
                {{ row.profit_loss.toFixed(2) }}%
              </el-tag>
            </template>
          </el-table-column>
        </el-table>
      </el-tab-pane>
      
      <el-tab-pane label="策略优化" name="strategies">
        <el-table :data="strategies">
          <el-table-column prop="name" label="策略名称" />
          <el-table-column label="胜率">
            <template #default="{ row }">
              {{ (row.performance_metrics.win_rate * 100).toFixed(1) }}%
            </template>
          </el-table-column>
          <el-table-column label="操作">
            <template #default="{ row }">
              <el-button size="small" @click="optimizeStrategy(row.strategy_id)">
                优化
              </el-button>
            </template>
          </el-table-column>
        </el-table>
      </el-tab-pane>
    </el-tabs>
  </div>
</template>
```

## 需要添加的路由

在 `alpha-council-vue/src/router/index.js` 中添加：

```javascript
{
  path: '/news',
  name: 'NewsAnalysis',
  component: () => import('@/components/NewsAnalysis.vue'),
  meta: { title: '新闻舆情' }
},
{
  path: '/debate',
  name: 'AgentDebate',
  component: () => import('@/components/AgentDebate.vue'),
  meta: { title: '智能体辩论' }
},
{
  path: '/trading',
  name: 'TradingSimulator',
  component: () => import('@/components/TradingSimulator.vue'),
  meta: { title: '模拟交易' }
},
{
  path: '/verification',
  name: 'VerificationSystem',
  component: () => import('@/components/VerificationSystem.vue'),
  meta: { title: '闭环验证' }
}
```

## 需要更新的导航菜单

在主菜单组件中添加：

```vue
<el-menu-item index="/news">
  <el-icon><Document /></el-icon>
  <span>新闻舆情</span>
</el-menu-item>
<el-menu-item index="/debate">
  <el-icon><ChatLineSquare /></el-icon>
  <span>智能辩论</span>
</el-menu-item>
<el-menu-item index="/trading">
  <el-icon><TrendCharts /></el-icon>
  <span>模拟交易</span>
</el-menu-item>
<el-menu-item index="/verification">
  <el-icon><CircleCheck /></el-icon>
  <span>闭环验证</span>
</el-menu-item>
```

## API配置

在 `alpha-council-vue/src/utils/request.js` 中确保：

```javascript
const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API || 'http://localhost:8000',
  timeout: 60000 // 60秒超时
})
```

## 安装依赖

```bash
cd alpha-council-vue
npm install echarts vue-echarts
npm install @element-plus/icons-vue
```

## 启动命令

```bash
# 启动后端
cd d:\AlphaCouncil
python backend\server.py

# 启动前端（新终端）
cd d:\AlphaCouncil\alpha-council-vue
npm run serve
```
