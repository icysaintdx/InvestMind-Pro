# 动态版本读取实现

> 完成时间: 2025-12-04 15:02  
> 版本: 1.3.3  
> 状态: ✅ 已实现

---

## 🎯 实现目标

将硬编码的版本信息改为动态读取，实现单一数据源管理

---

## 🏗️ 架构设计

```
src/
├── data/
│   └── changelog.js     # 版本信息的单一真相源
├── views/
│   └── ChangelogView.vue # 从changelog.js读取
└── App.vue              # 从changelog.js读取
```

---

## 📊 数据流

```
changelog.js
     ↓
┌─────────────┬─────────────┐
│   App.vue   │ChangelogView│
└─────────────┴─────────────┘
     ↓              ↓
  版本显示      完整更新日志
```

---

## ✅ 实现内容

### 1. 创建单一数据源 `changelog.js`

**位置**: `alpha-council-vue/src/data/changelog.js`

**内容**:
```javascript
export const CURRENT_VERSION = '1.3.3'
export const CURRENT_CODENAME = '多数据源适配版'

export const CHANGELOG_DATA = [
  {
    version: '1.3.3',
    codename: '多数据源适配版',
    date: '2025-12-04T14:48:00',
    features: [
      {
        icon: '📊',
        title: '多数据源格式支持',
        star: true,
        description: '完整支持5种主流数据源...',
        details: [...],
        files: [...]
      }
    ],
    bugs: [...]
  },
  // ... 其他版本
]

// 导出获取函数
export function getVersionInfo() {
  return {
    version: CURRENT_VERSION,
    codename: CURRENT_CODENAME,
    releaseDate: CHANGELOG_DATA[0]?.date
  }
}

export function getAllVersions() {
  return CHANGELOG_DATA
}
```

### 2. 修改 `App.vue`

**修改前**:
```javascript
const versionInfo = ref({
  version: '1.3.3',        // 硬编码
  codename: '多数据源适配版',
  releaseDate: '2025-12-04T14:48:00'
})
```

**修改后**:
```javascript
import { getVersionInfo } from './data/changelog.js'

const versionInfo = ref(getVersionInfo())  // 动态读取
```

### 3. 修改 `ChangelogView.vue`

**修改前**:
```javascript
data() {
  return {
    currentVersion: '1.3.3',  // 硬编码
    codename: '多数据源适配版',
    versions: [
      // ... 硬编码的版本数据
    ]
  }
}
```

**修改后**:
```javascript
import { CURRENT_VERSION, CURRENT_CODENAME, getAllVersions } from '../data/changelog.js'

data() {
  return {
    currentVersion: CURRENT_VERSION,     // 动态读取
    codename: CURRENT_CODENAME,         // 动态读取
    versions: getAllVersions()          // 动态读取
  }
}
```

---

## 📝 版本数据格式

### Features（新增功能）格式
```javascript
{
  icon: '📊',              // 图标
  title: '功能标题',
  star: true,             // 是否星标（重要功能）
  description: '功能描述',
  details: [              // 详细说明（可选）
    '详细点1',
    '详细点2'
  ],
  files: ['file1.py']     // 相关文件（可选）
}
```

### Bugs（问题修复）格式
```javascript
{
  icon: '🐛',              // 图标（可选，默认🔧）
  title: '修复标题',
  description: '问题描述',
  details: [...],          // 详细说明（可选）
  files: [...]            // 相关文件（可选）
}
```

---

## 💡 优势

### 1. 单一真相源
- 版本信息只在 `changelog.js` 中维护
- 避免多处更新的遗漏

### 2. 易于维护
- 新增版本只需在 `changelog.js` 添加一条记录
- 自动同步到所有使用的地方

### 3. 格式统一
- 所有版本信息格式一致
- 模板自动适应数据结构

### 4. 可扩展
- 可以添加更多字段（如下载链接、破坏性变更等）
- 可以从后端API读取（未来）

---

## 🚀 使用方法

### 更新版本时

1. **修改 `changelog.js`**:
```javascript
export const CURRENT_VERSION = '1.3.4'  // 更新版本号
export const CURRENT_CODENAME = '新版本名称'

export const CHANGELOG_DATA = [
  {
    version: '1.3.4',        // 新版本放在最前面
    codename: '新版本名称',
    date: '2025-12-05T10:00:00',
    features: [...],
    bugs: [...]
  },
  // ... 之前的版本
]
```

2. **修改 `package.json`**:
```json
{
  "version": "1.3.4"
}
```

3. **重启前端**:
```bash
npm run serve
```

---

## 📋 TODO

- [ ] 从后端 VERSION.json 动态读取
- [ ] 添加版本比较功能
- [ ] 添加版本搜索功能
- [ ] 支持版本回滚显示
- [ ] 添加破坏性变更(Breaking Changes)支持

---

## ⚠️ 注意事项

1. **日期格式**: 使用ISO 8601格式 `YYYY-MM-DDTHH:MM:SS`
2. **版本顺序**: 新版本放在数组最前面
3. **必需字段**: version, codename, date
4. **可选字段**: features, bugs, docs, breaking

---

**动态版本读取实现完成！** ✅

现在版本信息统一从 `changelog.js` 管理，避免了硬编码问题。
