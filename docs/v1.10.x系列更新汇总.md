# v1.10.x 系列更新汇总

**更新时间**: 2024-12-05 00:00 - 00:45  
**版本范围**: v1.10.0 - v1.10.5  
**主题**: GM白话功能、网络优化、模型配置、新闻过滤、提示词恢复

---

## 📊 版本概览

| 版本 | 发布时间 | 主要功能 | 文件数 |
|------|---------|---------|--------|
| v1.10.0 | 00:00 | GM卡片标签版 | 3 |
| v1.10.1 | 00:15 | 网络错误修复版 | 1 |
| v1.10.2 | 00:20 | 默认模型修复版 | 1 |
| v1.10.3 | 00:25 | 白话解读员配置版 | 1 |
| v1.10.4 | 00:30 | 模型配置和新闻过滤版 | 2 |
| v1.10.5 | 00:40 | 提示词恢复版 | 1 |

---

## 🎯 v1.10.0 - GM卡片标签版

**发布时间**: 2024-12-05 00:00

### 核心功能
在投资决策总经理（GM）的卡片中实现专业版和白话版的标签切换。

### 实现方式
1. **GM提示词改造**
   - 使用特殊标记分隔两个版本
   - `===PROFESSIONAL_START===` ... `===PROFESSIONAL_END===`
   - `===SIMPLE_START===` ... `===SIMPLE_END===`

2. **AgentCard解析逻辑**
   ```javascript
   parsedGMContent() {
     if (this.agent.id !== 'gm' || !this.output) {
       return { professional: this.output, simple: '', hasSimple: false }
     }
     const professionalMatch = this.output.match(/===PROFESSIONAL_START===([\s\S]*?)===PROFESSIONAL_END===/)
     const simpleMatch = this.output.match(/===SIMPLE_START===([\s\S]*?)===SIMPLE_END===/)
     return {
       professional: professionalMatch ? professionalMatch[1].trim() : this.output,
       simple: simpleMatch ? simpleMatch[1].trim() : '',
       hasSimple: !!simpleMatch
     }
   }
   ```

3. **标签栏UI**
   ```vue
   <div v-if="agent.id === 'gm' && parsedGMContent.hasSimple && output" class="gm-tab-bar">
     <button @click="currentView = 'professional'" :class="{active: currentView === 'professional'}">
       📊 专业版
     </button>
     <button @click="currentView = 'simple'" :class="{active: currentView === 'simple'}">
       📢 白话版
     </button>
   </div>
   ```

### 修改文件
- `AnalysisView.vue` - GM提示词修改
- `AgentCard.vue` - 添加解析和标签功能

### 视觉效果
```
┌──────────────────────────────────┐
│ 👑 投资决策总经理               │
├──────────────────────────────────┤
│ [📊 专业版] [📢 白话版]         │
├──────────────────────────────────┤
│ （显示选中的内容）               │
└──────────────────────────────────┘
```

---

## 🔧 v1.10.1 - 网络错误修复版

**发布时间**: 2024-12-05 00:15

### 修复问题

#### 1. SiliconFlow连接断开错误
**错误**: `httpcore.RemoteProtocolError: Server disconnected without sending a response.`

**修复方案**:
- 增加重试次数：2次 → 3次
- 添加指数退避：1秒 → 2秒 → 4秒
- 捕获更多异常：
  - `httpx.ReadTimeout`
  - `httpx.RemoteProtocolError` ← 新增
  - `httpx.ConnectError` ← 新增
  - `httpx.NetworkError` ← 新增

```python
max_retries = 3
for attempt in range(max_retries):
    try:
        response = await client.post(...)
        break
    except (httpx.ReadTimeout, httpx.RemoteProtocolError, httpx.ConnectError, httpx.NetworkError) as e:
        if attempt < max_retries - 1:
            wait_time = 2 ** attempt  # 指数退避
            await asyncio.sleep(wait_time)
        else:
            return {
                "success": True,
                "text": "⚠️ 由于网络问题，本次分析未能完成。建议：\n1. 检查网络连接\n2. 尝试使用其他 AI 模型\n3. 稍后重试",
                "timeout": True
            }
```

#### 2. DeepSeek 402错误
**错误**: `HTTP 402: DeepSeek API 错误`

**修复方案**:
```python
if response.status_code == 402:
    return {
        "success": True,
        "text": "⚠️ DeepSeek API 余额不足。建议：\n1. 检查 API 余额\n2. 切换到 SiliconFlow 或其他模型\n3. 充值后重试",
        "quota_exceeded": True
    }
```

### 修改文件
- `backend/server.py` - 增强错误处理

---

## 🔄 v1.10.2 - 默认模型修复版

**发布时间**: 2024-12-05 00:20

### 修复问题
白话解读员（interpreter）没有配置文件，使用了默认的DeepSeek导致402错误。

### 修复方案
修改默认模型为SiliconFlow：

```python
# 修复前
if not agent_config:
    agent_config = {
        "modelName": "deepseek-chat",  # ← DeepSeek余额不足
        "modelProvider": "DEEPSEEK",
        "temperature": 0.3
    }

# 修复后
if not agent_config:
    agent_config = {
        "modelName": "Qwen/Qwen2.5-7B-Instruct",  # ← 改为SiliconFlow
        "modelProvider": "SILICONFLOW",
        "temperature": 0.3
    }
```

### 修改文件
- `backend/server.py` - 修改默认模型

---

## ⚙️ v1.10.3 - 白话解读员配置版

**发布时间**: 2024-12-05 00:25

### 新增功能
在报告的白话版标签旁添加配置按钮，可以配置白话解读员的模型和温度。

### 实现方式

#### 1. 配置按钮
```vue
<button 
  v-if="reportView === 'simple'"
  @click="showInterpreterConfig = true; loadAvailableModels()"
  class="config-btn"
>
  ⚙️
</button>
```

#### 2. 配置弹窗
```vue
<div v-if="showInterpreterConfig" class="modal-overlay">
  <div class="interpreter-config-modal">
    <div class="modal-header">
      <h3>📢 白话解读员配置</h3>
    </div>
    <div class="modal-body">
      <select v-model="interpreterModel">
        <option value="Qwen/Qwen2.5-7B-Instruct">Qwen 2.5 7B (推荐)</option>
        ...
      </select>
      <input type="range" v-model.number="interpreterTemperature" min="0" max="1" step="0.1">
    </div>
    <div class="modal-footer">
      <button @click="saveInterpreterConfig">保存配置</button>
    </div>
  </div>
</div>
```

#### 3. 保存配置
```javascript
const saveInterpreterConfig = async () => {
  const config = await fetch('http://localhost:8000/api/agents/config').then(r => r.json())
  const interpreterConfig = {
    id: 'interpreter',
    modelName: interpreterModel.value,
    modelProvider: interpreterModel.value.includes('/') ? 'SILICONFLOW' : 'DEEPSEEK',
    temperature: interpreterTemperature.value
  }
  // 保存到文件
  await fetch('http://localhost:8000/api/agents/config', {
    method: 'POST',
    body: JSON.stringify({ ...config, agents: [...agents, interpreterConfig] })
  })
}
```

### 修改文件
- `AnalysisView.vue` - 添加配置功能

---

## 🎨 v1.10.4 - 模型配置和新闻过滤版

**发布时间**: 2024-12-05 00:30

### 修复问题1: 模型选择硬编码

**修复前**:
```vue
<option value="Qwen/Qwen2.5-7B-Instruct">Qwen 2.5 7B (推荐)</option>
<option value="deepseek-chat">DeepSeek Chat</option>
```

**修复后**:
```vue
<option v-for="model in availableModels" :key="model" :value="model">
  {{ model }}
</option>
```

```javascript
const loadAvailableModels = async () => {
  const response = await fetch('http://localhost:8000/api/agents/config')
  const config = await response.json()
  availableModels.value = config.selectedModels || []
}
```

### 新增功能: 新闻智能过滤

**过滤规则**:
1. 如果非中性新闻 >= 30篇 → 只显示非中性
2. 如果非中性新闻 < 30篇 → 非中性 + 部分中性 = 30篇
3. 如果总数 < 30篇 → 显示全部

**实现代码**:
```python
def _filter_news_by_sentiment(self, news_list: List[Dict]) -> Dict:
    positive_news = []
    negative_news = []
    neutral_news = []
    
    for news in news_list:
        score = self.sentiment_analyzer.analyze_text_sentiment(text)
        if score > 0.2:
            positive_news.append(news)
        elif score < -0.2:
            negative_news.append(news)
        else:
            neutral_news.append(news)
    
    non_neutral_count = len(positive_news) + len(negative_news)
    
    if non_neutral_count >= 30:
        filtered = positive_news + negative_news
        strategy = f'只显示非中性新闻 ({non_neutral_count}篇)'
    elif total_count <= 30:
        filtered = news_list
        strategy = f'总数不足，显示全部 ({total_count}篇)'
    else:
        need_neutral = 30 - non_neutral_count
        filtered = positive_news + negative_news + neutral_news[:need_neutral]
        strategy = f'非中性{non_neutral_count}篇 + 中性{need_neutral}篇'
    
    return {'news': filtered, 'strategy': strategy}
```

### 修改文件
- `AnalysisView.vue` - 动态加载模型
- `unified_news_api.py` - 添加过滤逻辑

---

## 📝 v1.10.5 - 提示词恢复版

**发布时间**: 2024-12-05 00:40

### 修复问题
智能体提示词被简化，导致分析质量下降。

### 恢复的提示词

#### 新闻舆情分析师
```javascript
news_analyst: `你是一位专业的新闻舆情分析师。请完成以下任务：
1. 主动搜索并分析该股票最近24-48小时的所有相关新闻、公告、研报
2. 识别可能影响股价的关键事件（业绩、政策、行业动态、重大合同等）
3. 评估新闻的情绪倾向（利好/利空/中性），并给出情绪评分（-10到10）
4. 分析新闻的可信度和影响力（权威媒体vs自媒体）
5. 总结核心观点：当前舆情是偏多还是偏空？

注意：
- 必须给出具体的新闻内容和分析，不要说"暂无重大事件"
- 即使没有重大新闻，也要分析常规新闻和市场讨论
- 明确区分利好、利空和中性新闻
- 给出整体情绪评分和建议`
```

#### 其他智能体
- 社交媒体分析师 - 5步分析流程
- 中国市场专家 - 5个维度分析
- 行业研究专家 - 5步分析流程
- 宏观经济学家 - 5步分析流程
- 技术分析师 - 6步分析流程
- 资金流向分析师 - 6步分析流程
- 基本面分析师 - 6步分析流程

### 提示词对比

| 智能体 | 简化版 | 详细版 | 增加 |
|--------|--------|--------|------|
| news_analyst | 35字 | 280字 | +700% |
| social_analyst | 30字 | 180字 | +500% |
| technical | 45字 | 220字 | +388% |
| funds | 30字 | 190字 | +533% |

### 修改文件
- `AnalysisView.vue` - 恢复8个智能体的详细提示词

---

## 📊 总体统计

### 修改文件统计
- `AnalysisView.vue` - 5次修改
- `AgentCard.vue` - 2次修改
- `StyleConfig.vue` - 2次修改
- `backend/server.py` - 3次修改
- `unified_news_api.py` - 1次修改

### 代码行数变化
- 新增代码：约 800 行
- 修改代码：约 200 行
- 删除代码：约 50 行

### 功能统计
- 新增功能：5个
- Bug修复：4个
- 优化改进：3个

---

## 🎯 核心改进

### 1. 用户体验
- ✅ GM卡片标签切换 - 专业版和白话版
- ✅ 白话解读员配置 - 可自定义模型
- ✅ 模型动态加载 - 从配置文件读取

### 2. 稳定性
- ✅ 网络错误处理 - 3次重试+指数退避
- ✅ API余额处理 - 友好的降级响应
- ✅ 默认模型优化 - 避免余额不足

### 3. 智能化
- ✅ 新闻智能过滤 - 优先显示非中性
- ✅ 提示词优化 - 详细的分析流程

---

## 🚀 使用建议

### 1. 重启服务
```bash
# 停止后端
Ctrl + C

# 重启后端
python backend/server.py

# 刷新前端
Ctrl + F5
```

### 2. 配置检查
- 检查 `agent_configs.json` 的 `selectedModels`
- 确认白话解读员配置已保存
- 验证网络连接稳定性

### 3. 功能测试
- 测试GM卡片标签切换
- 测试白话解读员配置
- 测试新闻过滤效果
- 观察网络错误处理

---

## 📝 相关文档

- [v1.10.0 GM卡片标签版](./v1.10.0GM卡片标签版.md)
- [v1.10.1 网络错误修复版](./v1.10.1网络错误修复版.md)
- [v1.10.2 默认模型修复版](./v1.10.2默认模型修复版.md)
- [v1.10.3 白话解读员配置版](./v1.10.3白话解读员配置版.md)
- [v1.10.4 模型配置和新闻过滤版](./v1.10.4模型配置和新闻过滤版.md)
- [v1.10.5 提示词恢复版](./v1.10.5提示词恢复版.md)

---

## 🎉 总结

v1.10.x系列更新专注于：
1. **用户体验优化** - GM白话功能、配置界面
2. **稳定性提升** - 网络错误处理、API降级
3. **智能化增强** - 新闻过滤、提示词优化

**现在系统更稳定、更智能、更友好！**🎉
