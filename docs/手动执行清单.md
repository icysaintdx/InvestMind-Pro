# ✅ AlphaCouncil 手动执行清单

## 🔧 手动执行命令

### 🚀 快速开始（推荐）
```bash
# 方式1：前后端一体启动（推荐）
START_ALL.bat

# 方式2：仅启动后端（用于测试）
LAUNCH_SERVER.bat

# 停止所有服务
STOP_ALL.bat
```

### 🔍 如需单独检查和修复
```bash
# 1. 检查系统状态
python FINAL_STATUS.py

# 2. 测试配置工具修复
python test_config_utils_fix.py

# 3. 测试API导入修复
python test_api_import_fix.py

# 4. 修复NumPy/ChromaDB兼容性
python fix_chromadb_auto.py

# 5. 启动服务器
python backend/server.py
```

### 0. 安装项目依赖（如果快速开始失败，手动执行）
```bash
# 方式1：使用批处理脚本
install_dependencies.bat

# 方式2：使用Python脚本
python install_all.py

# 方式3：手动安装核心依赖
pip install colorlog colorama termcolor
pip install fastapi uvicorn httpx python-dotenv
pip install pandas numpy akshare tushare

# 如果使用LangChain相关功能，还需要：
install_langchain.bat
# 或
pip install langchain langchain-core langchain-openai langchain-community
```

### 0.1 修复所有导入问题（重要）
```bash
# 修复logging和tradingagents导入问题
python fix_all_imports_v2.py
```

### 1. 环境准备
```bash
cd d:\AlphaCouncil
mkdir backend\data
```

### 2. 测试导入路径修复
```bash
python scripts\fix_imports.py
```
预期输出：修复了backend/agents, backend/dataflows, backend/api中的文件

### 3. 测试后端服务器启动
```bash
# 推荐：使用启动脚本（自动设置PYTHONPATH）
start_backend.bat

# 或直接运行
cd d:\AlphaCouncil
python backend\server.py
```
访问 http://localhost:8000/docs 查看API文档

### 3.1 测试智能体管理API
```bash
# 服务器启动后，在浏览器访问：
http://localhost:8000/api/agents/registry   # 查看所有20个智能体
http://localhost:8000/api/agents/list       # 智能体列表
http://localhost:8000/api/agents/workflow/stages  # 工作流阶段
http://localhost:8000/api/agents/statistics  # 统计信息
```

### 4. 测试LLM集成
```bash
# 确保服务器已启动后
python scripts\test_llm_integration.py
```

### 5. 测试所有API端点
```bash
# 确保服务器已启动后
python scripts\test_all_apis.py
```

### 6. 一键启动（包含前后端）
```bash
quick_start.bat
```

## 📝 需要检查的文件

确认以下关键文件已创建：

### API模块 (backend/api/)
- [x] news_api.py - 新闻分析API
- [x] debate_api.py - 多智能体辩论API  
- [x] trading_api.py - 模拟交易API
- [x] verification_api.py - 闭环验证API

### 数据流 (backend/dataflows/)
- [x] china_market_crawler.py - 中国市场爬虫

### 工具类 (backend/utils/)
- [x] logging_config.py - 统一日志系统
- [x] tool_logging.py - 日志装饰器
- [x] stock_utils.py - 股票工具类

### 测试脚本 (scripts/)
- [x] fix_imports.py - 导入路径修复
- [x] test_all_apis.py - API测试脚本
- [x] reorganize_direct.py - 目录重组工具

## 🚀 下一步操作

### 1. 安装缺失的Python包（如果需要）
```bash
pip install fastapi uvicorn httpx
pip install akshare tushare
pip install beautifulsoup4 lxml
pip install numpy pandas
```

### 2. 配置环境变量（.env文件）
```env
# AI模型API密钥
DEEPSEEK_API_KEY=your_key_here
QWEN_API_KEY=your_key_here
DASHSCOPE_API_KEY=your_key_here

# 数据API密钥
TUSHARE_TOKEN=your_token_here
JUHE_API_KEY=your_key_here
```

### 3. 前端Vue组件集成
参考 `docs/前端集成指南.md`，手动创建Vue组件：
- NewsAnalysis.vue - 新闻分析组件
- AgentDebate.vue - 智能体辩论组件
- TradingSimulator.vue - 模拟交易组件
- VerificationSystem.vue - 闭环验证组件

### 4. 更新Vue路由
在 `alpha-council-vue/src/router/index.js` 添加新路由

### 5. 安装前端依赖
```bash
cd alpha-council-vue
npm install echarts vue-echarts
npm install @element-plus/icons-vue
```

## 🎯 验证清单

### 后端验证
- [ ] 服务器能正常启动
- [ ] http://localhost:8000/docs 能访问
- [ ] 所有API端点测试通过

### 前端验证
- [ ] Vue项目能正常启动
- [ ] http://localhost:8080 能访问
- [ ] 新组件能正常显示

### 功能验证
- [ ] 新闻数据能获取
- [ ] 辩论功能能触发
- [ ] 模拟交易能执行
- [ ] 验证系统能记录

## ⚠️ 常见问题

### 1. 导入错误
如果出现 `ModuleNotFoundError`：
```bash
python scripts\fix_imports.py
```

### 2. 端口占用
如果8000或8080端口被占用：
```bash
# 查找并结束占用进程
netstat -ano | findstr :8000
taskkill /PID <进程ID> /F
```

### 3. 依赖缺失
如果提示缺少某个包：
```bash
pip install <包名>
```

## 📊 项目状态

- **完成度**: 70%
- **版本**: v2.0.0-beta
- **核心模块**: ✅ 全部完成
- **待完成**: LLM对接、前端组件、数据持久化

## 💡 提示

1. **先测试后端**，确保API正常工作
2. **逐步集成前端**，每个组件单独测试
3. **保存好日志**，方便问题排查
4. **定期提交Git**，保存进度

---

执行完以上步骤后，AlphaCouncil v2.0.0-beta 就可以正常运行了！
