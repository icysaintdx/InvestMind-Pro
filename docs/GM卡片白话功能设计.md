# GM卡片白话功能设计方案

## 🎯 设计目标

在投资决策总经理（GM）的卡片中，提供专业版和白话版两种查看方式。

## 📊 智能体职责

### 投资决策总经理（GM）
- **职责**: 最终投资决策
- **输出**: 买入/卖出/观望 + 目标价位 + 仓位建议
- **地位**: 最终决策者

### 量化交易员（Trader）
- **职责**: 执行计划制定
- **输出**: 入场点位 + 止损位 + 目标位 + 分批策略
- **地位**: 执行者

## 🎨 推荐方案：卡片内标签切换

### 视觉效果

```
┌──────────────────────────────────────────┐
│ 👑 投资决策总经理              ⚙️ 🔍    │
├──────────────────────────────────────────┤
│ [📊 专业版] [📢 白话版]                  │  ← 标签栏
├──────────────────────────────────────────┤
│                                          │
│  （当前选中标签的内容）                   │
│                                          │
│  专业版：完整的投资决策分析               │
│  白话版：通俗易懂的买卖建议               │
│                                          │
└──────────────────────────────────────────┘
```

### 交互流程

```
1. 默认显示专业版
2. 点击"白话版"标签 → 切换到白话内容
3. 点击"专业版"标签 → 切换回专业内容
4. 状态保存（可选）
```

## 💻 实现方案

### 1. 修改GM的提示词

```javascript
gm: `作为投资决策总经理，给出最终投资决策。

请按以下格式输出，用特殊标记分隔两个版本：

===PROFESSIONAL_START===
## 专业投资决策

### 投资建议
- 决策结论：（买入/卖出/观望）
- 目标价位：
- 仓位建议：
- 投资周期：

### 决策依据
（基于所有分析师的专业意见）

### 风险评估
（综合风控团队的评估）
===PROFESSIONAL_END===

===SIMPLE_START===
## 白话投资建议

### 📊 能买不？
（明确回答：强烈推荐/可以买/观望/不建议）

### 💰 价格指引
- 买入价：（具体价格）
- 卖出价：（具体价格）
- 持有期：（时间）

### ⚠️ 风险提醒
1. （大白话说风险）
2. （大白话说风险）
3. （大白话说风险）

### 📝 怎么操作
第1步：
第2步：
第3步：
===SIMPLE_END===
`
```

### 2. 解析输出

```javascript
// 在AgentCard中解析GM的输出
const parseGMOutput = (output) => {
  const professionalMatch = output.match(/===PROFESSIONAL_START===([\s\S]*?)===PROFESSIONAL_END===/);
  const simpleMatch = output.match(/===SIMPLE_START===([\s\S]*?)===SIMPLE_END===/);
  
  return {
    professional: professionalMatch ? professionalMatch[1].trim() : output,
    simple: simpleMatch ? simpleMatch[1].trim() : ''
  };
};
```

### 3. 卡片UI

```vue
<template>
  <div class="agent-card">
    <!-- 卡片头部 -->
    <div class="card-header">
      <span>👑 投资决策总经理</span>
    </div>
    
    <!-- 标签栏（仅GM显示） -->
    <div v-if="agent.id === 'gm' && parsedContent.simple" class="tab-bar">
      <button 
        @click="currentView = 'professional'" 
        :class="{active: currentView === 'professional'}"
        class="tab-btn"
      >
        📊 专业版
      </button>
      <button 
        @click="currentView = 'simple'" 
        :class="{active: currentView === 'simple'}"
        class="tab-btn"
      >
        📢 白话版
      </button>
    </div>
    
    <!-- 内容区 -->
    <div class="card-content">
      <div v-if="agent.id === 'gm' && parsedContent.simple">
        <!-- GM的双版本内容 -->
        <div v-show="currentView === 'professional'" class="professional-content">
          <TypeWriter :text="parsedContent.professional" />
        </div>
        <div v-show="currentView === 'simple'" class="simple-content">
          <TypeWriter :text="parsedContent.simple" />
        </div>
      </div>
      <div v-else>
        <!-- 其他智能体的正常内容 -->
        <TypeWriter :text="output" />
      </div>
    </div>
  </div>
</template>

<script>
export default {
  data() {
    return {
      currentView: 'professional' // 默认显示专业版
    };
  },
  computed: {
    parsedContent() {
      if (this.agent.id === 'gm') {
        return this.parseGMOutput(this.output);
      }
      return { professional: this.output, simple: '' };
    }
  }
};
</script>
```

### 4. 样式

```css
.tab-bar {
  display: flex;
  gap: 8px;
  padding: 10px;
  border-bottom: 1px solid rgba(71, 85, 105, 0.3);
}

.tab-btn {
  padding: 8px 16px;
  background: rgba(51, 65, 85, 0.5);
  border: none;
  border-radius: 8px;
  color: #94a3b8;
  cursor: pointer;
  transition: all 0.3s ease;
}

.tab-btn:hover {
  background: rgba(71, 85, 105, 0.5);
  color: #e2e8f0;
}

.tab-btn.active {
  background: rgba(59, 130, 246, 0.3);
  color: #60a5fa;
  font-weight: 600;
}

.simple-content {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(5, 150, 105, 0.1) 100%);
  padding: 15px;
  border-radius: 10px;
}
```

## ✅ 优势

1. **直观** - 一眼看到两个版本
2. **方便** - 点击即切换
3. **清晰** - 知道当前看的是哪个版本
4. **不干扰** - 其他智能体不受影响
5. **专业** - 默认显示专业版

## 🎯 与其他方案对比

| 特性 | 卡片标签 | 按钮弹窗 | 开关控制 |
|------|---------|---------|---------|
| 直观性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 便捷性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 空间占用 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 清晰度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

## 🚀 实施步骤

1. 修改GM的提示词，输出双版本内容
2. 在AgentCard中添加解析逻辑
3. 添加标签栏UI（仅GM显示）
4. 添加标签切换逻辑
5. 添加样式美化

## 📝 注意事项

1. 只有GM卡片显示标签栏
2. 其他智能体保持原样
3. 默认显示专业版
4. 白话版用绿色主题区分
5. 打字机效果保持不变
