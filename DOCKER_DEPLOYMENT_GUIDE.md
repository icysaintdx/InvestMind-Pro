# Docker 部署方案选择指南

## 🎯 三种部署方案

### 方案 1: 一体化部署 ⭐⭐⭐⭐⭐ (推荐个人使用)

**特点**：
- ✅ 一个镜像包含前后端
- ✅ 一条命令启动
- ✅ 只需要一个端口(80)
- ✅ 最简单

**适用场景**：
- 个人使用
- 家庭 NAS
- 快速测试
- 不需要独立扩展

**构建**：
```bash
docker-build-all-in-one.bat
```

**部署**：
```bash
docker run -d -p 80:80 --env-file .env alphacouncil:latest
```

**文档**: `NAS_ALL_IN_ONE.md`

---

### 方案 2: 分离部署（docker-compose）⭐⭐⭐⭐ (推荐生产环境)

**特点**：
- ✅ 前后端独立镜像
- ✅ 可以独立扩展
- ✅ 更灵活
- ⚠️ 需要配置网络

**适用场景**：
- 生产环境
- 需要独立扩展前后端
- 多实例部署
- 专业运维

**构建**：
```bash
docker-build-offline.bat
```

**部署**：
```bash
docker-compose up -d
```

**文档**: `docs/Docker部署指南.md`

---

### 方案 3: 独立容器部署 ⭐⭐⭐ (NAS 推荐)

**特点**：
- ✅ 前后端独立镜像
- ✅ 不依赖 docker-compose
- ✅ 适合 NAS
- ⚠️ 需要手动管理

**适用场景**：
- NAS 部署
- 不支持 docker-compose
- 需要独立管理容器

**构建**：
```bash
docker-build-for-nas.bat
```

**部署**：
```bash
# 后端
docker run -d -p 8000:8000 alphacouncil-backend:latest

# 前端
docker run -d -p 80:80 alphacouncil-frontend:latest
```

**文档**: `NAS_QUICK_START.md`

---

## 📊 详细对比

| 特性 | 一体化 | docker-compose | 独立容器 |
|------|--------|----------------|----------|
| **镜像数量** | 1个 | 2个 | 2个 |
| **端口** | 80 | 80+8000 | 80+8000 |
| **启动命令** | 1条 | 1条 | 2条 |
| **网络配置** | 无需 | 自动 | 手动 |
| **镜像大小** | ~1.5GB | ~700MB | ~700MB |
| **构建时间** | 10-15分钟 | 10-15分钟 | 10-15分钟 |
| **维护难度** | ⭐ 简单 | ⭐⭐ 中等 | ⭐⭐⭐ 较难 |
| **扩展性** | ❌ 低 | ✅ 高 | ✅ 高 |
| **资源占用** | 中等 | 低 | 低 |

---

## 🎯 快速选择

### 我应该选择哪个？

#### 选择一体化部署，如果你：
- ✅ 只是个人使用
- ✅ 想要最简单的部署
- ✅ 不需要独立扩展
- ✅ 使用家庭 NAS

#### 选择 docker-compose，如果你：
- ✅ 生产环境部署
- ✅ 需要独立扩展前后端
- ✅ 有专业运维团队
- ✅ 需要高可用

#### 选择独立容器，如果你：
- ✅ NAS 不支持 docker-compose
- ✅ 需要精细控制每个容器
- ✅ 想要独立管理

---

## 📝 构建脚本对照表

| 脚本 | 方案 | 输出 |
|------|------|------|
| `docker-build-all-in-one.bat` | 一体化 | `alphacouncil-all-in-one.tar` |
| `docker-build-offline.bat` | docker-compose | `alphacouncil-backend.tar` + `alphacouncil-frontend.tar` |
| `docker-build-for-nas.bat` | 独立容器 | `alphacouncil-backend.tar` + `alphacouncil-frontend.tar` |

---

## 🚀 推荐流程

### 个人用户（最简单）

```bash
# 1. 构建
docker-build-all-in-one.bat

# 2. 本地测试
docker run -p 80:80 --env-file .env alphacouncil:latest

# 3. 上传到 NAS
# 上传 alphacouncil-all-in-one.tar

# 4. NAS 部署
docker load -i alphacouncil-all-in-one.tar
docker run -d -p 80:80 --env-file .env alphacouncil:latest
```

### 生产环境（最稳定）

```bash
# 1. 构建
docker-build-offline.bat

# 2. 本地测试
docker-compose up -d

# 3. 部署到服务器
# 上传 docker-compose.yml 和 tar 文件

# 4. 服务器部署
docker-compose up -d
```

---

## 💡 常见问题

### Q: 一体化部署性能会差吗？
A: 不会。前后端都在同一容器内，通过 localhost 通信，反而延迟更低。

### Q: 一体化部署可以扩展吗？
A: 可以运行多个容器，但前后端无法独立扩展。

### Q: 哪个方案最省资源？
A: docker-compose 和独立容器，因为镜像更小。

### Q: 哪个方案最简单？
A: 一体化部署，一条命令搞定。

### Q: 生产环境推荐哪个？
A: docker-compose，更灵活，更专业。

---

## 📚 相关文档

- **一体化部署**: `NAS_ALL_IN_ONE.md`
- **docker-compose**: `docs/Docker部署指南.md`
- **独立容器**: `NAS_QUICK_START.md`
- **网络问题**: `DOCKER_NETWORK_ISSUES.md`

---

## ✅ 我的推荐

### 🏠 家庭/个人 NAS
```bash
docker-build-all-in-one.bat
```
→ 最简单，一个文件，一条命令

### 🏢 公司/生产环境
```bash
docker-build-offline.bat
```
→ 最专业，可扩展，易维护

### 🔧 特殊需求
```bash
docker-build-for-nas.bat
```
→ 最灵活，完全控制

---

## 🎉 开始部署！

选择适合你的方案，运行对应的构建脚本，然后按照文档部署即可！
